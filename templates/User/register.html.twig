{% extends "index.html.twig" %}
{% block title %}{{ parent() }} - Connectez-vous {% endblock %}
{% block body %}

    <div class="container-fluid mt-2">
        {% if session.errorlogin is defined %}
            <div class="alert alert-danger">{{ session.errorlogin }}</div>
        {% endif %}
        <form method="post" action="/Register">

            <p>Prénom :</p>
            <input name="u_prenom" class="form-control form-control-lg">
            <p>sexe :</p>
            <input  name="sexe" class="form-control form-control-lg">
            <p>Nom :</p>
            <input name="u_nom" class="form-control form-control-lg">
            <p>Mot de passe :</p>
            <input type="password" name="password" class="form-control form-control-lg">
            <p>Ville :</p>
            <input  name="ville" class="form-control form-control-lg">
            <p>telephone :</p>
            <input  name="telephone" class="form-control form-control-lg">
            <p>age :</p>
            <input  name="age" class="form-control form-control-lg">
            <p>Prefhum :</p>
            <input  name="prefhum"  class="form-control form-control-lg">
            <p>statut :</p>
            <input  name="statut" class="form-control form-control-lg">
            <p>Adresse e-mail :</p>
            <input type="email" name="email" class="form-control form-control-lg">

            <br>
            <input type="submit">
        </form>
    </div>

{% endblock %}